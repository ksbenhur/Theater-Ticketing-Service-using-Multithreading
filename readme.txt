ReadMe File
----------------
Compilation:

To compile the program, use the following command:

gcc *.c -lpthread -o main

Execution:

To run the program, use the command:

./main <num>

Example:

For example, to simulate a scenario where 5 customers arrive at random intervals for each seller’s queue, you would run:

./main 5